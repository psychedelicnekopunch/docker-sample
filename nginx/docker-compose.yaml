# web:
#   image: nginx
#   volumes:
#    - ./templates:/etc/nginx/templates
#   ports:
#    - "8080:80"
#   environment:
#    - NGINX_HOST=foobar.com
#    - NGINX_PORT=80

services:
    nginx:
        build:
            context: .
        volumes:
         # - .:/usr/share/nginx/html/prod
         - .:/var/www/html/prod
        ports:
         - "8080:80"
        # networks:
        #     default:
        #         aliases:
        #             - local2.sample
